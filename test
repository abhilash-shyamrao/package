CREATE TABLE access_group (
    access_id SERIAL PRIMARY KEY,
    access_role_name VARCHAR(255),
    access_description TEXT,
    created_on TIMESTAMP,
    created_by VARCHAR(50),
    last_updated_on TIMESTAMP,
    last_updated_by VARCHAR(50)
);
